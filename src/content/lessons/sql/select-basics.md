# SELECT Basics

`SELECT` is used to retrieve data from a table.

## Example

```sql
SELECT name, age
FROM users;
```

This query returns the `name` and `age` columns from the `users` table.

## Why it matters

Most SQL interview questions start with selecting the right columns before adding filters, grouping, or joins.